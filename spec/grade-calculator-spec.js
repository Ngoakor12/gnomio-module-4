const { calculateLetterGrade } = require("../src/grade-calculator");
describe("it takes in a numeric grade and returns its letter grade equivalent", () => {
	// it("checks if the numericGrade is in range", () => {
	// 	numericGrade = 6;
	// 	expect(function () {
	// 		calculateLetterGrade(numericGrade);
	// 	}).toThrowError("Input should be a number and not null");
	// });
	// it("checks if the 1.0 returns A", () => {
	// 	numericGrade = 1.0;
	// 	expect(calculateLetterGrade(numericGrade)).toBe("A");
	// });
});
